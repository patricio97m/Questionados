<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jugar</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
<main class="container mt-3 rounded col-md-6 text-center bg-dark card border-{{categoriaEstilo}}">
    <div class="bg-{{categoriaEstilo}} card-header rounded d-flex align-items-center">
        <img src="../public/{{categoria}}.svg" alt="categoria" class="svg-icon">
        <h3 class="text-light mx-auto">{{categoria}}</h3>
    </div>
    <div class="container mt-5">
        <h1 class="border rounded p-4 bg-light">{{pregunta}}</h1>

        <div class="mt-5">
            {{#respuestas}}
                <button class="btn btn-light btn-block btn-lg">{{respuesta}}</button>
            {{/respuestas}}
        </div>

        <div class="mt-3 mb-3">
            <h5 class="text-left text-light">Puntaje: 1</h5>
        </div>
    </div>
</main>
</body>
